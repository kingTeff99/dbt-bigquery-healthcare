name: "healthcare_dbt_bigquery_data_pipeline"
version: "1.0.0"
profile: "healthcare_dbt_bigquery_data_pipeline"
config-version: 2
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
clean-targets:
  - "target"
  - "dbt_packages"
models:
  healthcare_dbt_bigquery_data_pipeline:
    +materialized: view
vars:
  schema_name: "dev_healthcare_data"